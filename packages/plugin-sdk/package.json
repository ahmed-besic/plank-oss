{
  "name": "@plank/plugin-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"No lint step for @plank/plugin-sdk\"",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --check src",
    "check": "prettier --check src"
  },
  "dependencies": {
    "@plank/domain": "workspace:*",
    "react": "^19.2.0"
  }
}
